# Metric Transform Samples

NOT VERIFIED locally: server P1 metric_v2 `meta/*_architecture.json` files are not present in this local snapshot. Run `scripts/qwen_arch_control/audit_metric_transform_contract.py` on A800 to populate per-sample keys and transforms.
