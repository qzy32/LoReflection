# Semantic Footprint V1

Canonical furniture representation is the simplified instance footprint mask. bbox/AABB is an auxiliary field derived from the mask, not the target representation. The semantic layout loop does not output explicit yaw, rotY, front direction, or facing results; those belong to APM lifting.
